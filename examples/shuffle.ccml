function main()
    var d[4]
    d[0] = 0
    d[1] = 1
    d[2] = 2
    d[3] = 3
    var i
    for (i = 0 to 4)
        var j = rand() % 4
        var t = d[j]
        d[j] = d[i]
        d[i] = t
    end
    
    puts(d[0] + ", " + d[1] + ", " + d[2] + ", " + d[3])
end
