#expect success

function main()
    var x

    x = abs(-3)
    if (not x == 3)
        return -1
    end
    
    x = abs(3)
    if (not x == 3)
        return -2
    end
    
    x = min(1, 4)
    if (not x == 1)
        return -3
    end
    
    x = min(-1, -4)
    if (not x == -4)
        return -4
    end

    x = max(1, 4)
    if (not x == 4)
        return -5
    end
    
    x = max(-1, -4)
    if (not x == -1)
        return -6
    end
    
    puts("success")
end
